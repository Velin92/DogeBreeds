platform :ios, '15.0'

target 'DogeBreeds' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DogeBreeds
  pod 'Alamofire', '~> 5.5'
  pod 'Kingfisher', '~> 7.0'

  target 'DogeBreedsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DogeBreedsUITests' do
    # Pods for testing
  end

end
